apiVersion: v1
kind: Pod
metadata:
  name: conv-app
  labels:
    environment: production
spec:
  containers:
    - name: conv-app
      image: antonlarichev/conv-app:latest
      ports:
        - containerPort: 3000
      resources:
        limits:
          memory: "128Mi"
          cpu: "500m"


